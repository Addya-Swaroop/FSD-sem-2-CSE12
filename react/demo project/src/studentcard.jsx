function StudentCard({name,age,grade}:){
    return(
        <div>
            <h2>name:{name}</h2>
            <p>age: {age}</p>
            <p>Grade: {grade}</p>
        </div>
    );
}
export default Studentcard;